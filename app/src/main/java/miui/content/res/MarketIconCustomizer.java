package miui.content.res;

/**
 * Presence marker expected by Xiaomi Market when checking MIUI launcher icon support.
 * AOSP/crDroid has no MIUI launcher, so the Market keeps the feature disabled.
 */
public final class MarketIconCustomizer {
    private MarketIconCustomizer() {
    }
}
